// Must be the first import (required by react-native-gesture-handler on Android).
import 'react-native-gesture-handler';
import { registerRootComponent } from 'expo';
import App from './App';

registerRootComponent(App);
